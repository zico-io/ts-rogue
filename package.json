{
  "name": "ts-rogue",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A TypeScript terminal dungeon crawler",
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "check": "pnpm typecheck && pnpm test && pnpm docs:check && pnpm lint",
    "eve:dev": "eve dev --no-ui",
    "game": "tsx src/app.tsx",
    "game:dev": "tsx src/app.tsx --dev",
    "prepare": "git config core.hooksPath .githooks || true",
    "docs:check": "node scripts/check-docs.mjs",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "test": "pnpm test:unit && node --test scripts/check-docs.test.mjs",
    "lint": "biome ci src && biome lint agent",
    "format": "biome format --write ."
  },
  "dependencies": {
    "@vercel/connect": "0.4.0",
    "ai": "^7.0.26",
    "eve": "^0.25.2",
    "ink": "^7",
    "react": "^19",
    "rot-js": "^2",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2",
    "@types/node": "^24.13.3",
    "@types/react": "^19",
    "tsx": "^4",
    "typescript": "^7",
    "vitest": "^4"
  }
}
